<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.Yelp" parent="Theme.MaterialComponents.DayNight.DarkActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/red</item>
        <item name="colorPrimaryVariant">@color/red</item>
        <item name="colorOnPrimary">@color/white</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/teal_200</item>
        <item name="colorSecondaryVariant">@color/teal_700</item>
        <item name="colorOnSecondary">@color/ic_yelp_logo_background</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor">@color/red</item>
        <!-- Customize your theme here. -->
    </style>
    <style name="Theme.Yelp.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
        <item name="android:statusBarColor">@color/white</item>
    </style>
    <style name="Theme.AppCompat.Light.DarkActionBar">
        <item name="android:statusBarColor">#ffffff</item>
    </style>
    <style name="Theme.Yelp.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />
    <style name="Theme.Yelp.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />
</resources>